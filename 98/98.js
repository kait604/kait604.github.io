const $test = new $Window({
  title: 'Test',
  outerWidth: 400,
  outerHeight: 300,
  resizable: true,
})

while 1=1 {
  $window.css({
				left: x,
				top: y,
				// width: width,
				// height: height,
			});
}
